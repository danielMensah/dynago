## Codebase Patterns
- `Eval(node, item)` evaluates condition/filter AST; `EvalUpdate(nodes, item)` applies update AST returning a new item copy
- `resolvePathValue(parts, item)` traverses nested M attributes for dotted paths; reused by both evaluators
- `resolveNode(node, item)` resolves PathNode/ValueNode/FuncNode(size) to AttributeValue; used in comparisons and updates
- DynamoDB number comparisons: parse string N values to float64 via strconv.ParseFloat
- Update evaluator uses copy-on-write for nested maps to avoid mutating input items
- Package name is `dynago`, all files in project root
- No external dependencies yet; stdlib only
- Tests use standard `testing` package with table-driven tests
- Struct tag name is `dynamo` (not `dynago`); parsed via `parseTag()` in codec.go
- `getCodec(reflect.Type)` returns cached `*typeCodec` via `sync.Map`; use this for marshal/unmarshal
- `fieldOptions` contains all tag metadata including GSI/key info for future phases
- Known tag options: field name, hash, range, gsi:<name>, omitempty, set, unixtime, - (skip)
- `Backend` interface in backend.go defines the 10 DynamoDB methods; all request/response types are library-owned
- `Table.Backend()` and `Table.Name()` are the accessors free functions (Get[T], Query[T]) use
- `KeyValue.Map()` returns the internal map[string]AttributeValue for passing to Backend
- `Key()` panics on misuse (programmer error convention like regexp.MustCompile)
- `Marshal(v any)` in marshal.go converts structs to `map[string]AttributeValue` using cached typeCodec
- `Marshaler` interface (`MarshalDynamo() (AttributeValue, error)`) for custom types
- `marshalValue(reflect.Value, fieldOptions)` is the recursive value converter
- nil pointers are omitted (not encoded as NULL); nil slices/maps encode as NULL
- `isZero()` helper handles omitempty logic for all types including time.Time
- Expression AST lives in `internal/expr/` package; Node interface has private marker method
- `ParseCondition(expr, args...)` parses placeholder expressions into AST with correct precedence (NOT > AND > OR)
- `BuildSet/BuildAdd/BuildRemove/BuildDelete` construct UpdateNode for update expressions
- `goToAttributeValue(v any)` in builder.go converts Go values to dynago.AttributeValue
- Reserved words in `internal/reserved.go` as `map[string]bool` with uppercase keys; use `IsReserved(word)` for lookup
- `ToDynamo(node)` returns (exprString, names, values); `ToDynamoUpdates([]UpdateNode)` groups by action; `ToDynamoProjection(node)` for projections
- IMPORT CYCLE: `internal/expr` imports `dynago` (for AttributeValue in ValueNode). Do NOT import `internal/expr` from the `dynago` package. Build projection/condition expressions inline instead.
- `buildProjection(attrs)` in get.go builds projection expressions without importing internal/expr
- All attribute names get `#name` aliases in expression translation, not just reserved words
- Value placeholders use `:v0`, `:v1`, etc. with incrementing counter
- Logical AND/OR wrap operands in parens: `(left) AND (right)`; NOT wraps operand: `NOT (operand)`
- `Unmarshal(item, &out)` in unmarshal.go decodes `map[string]AttributeValue` into structs using cached typeCodec
- `Unmarshaler` interface (`UnmarshalDynamo(AttributeValue) error`) for custom types, checked via pointer receiver first
- `unmarshalValue(av, dst, opts)` is the recursive value decoder; handles pointers, sets, maps, nested structs
- `reflect.TypeFor[T]()` preferred over `reflect.TypeOf((*T)(nil)).Elem()` for type comparisons

---

## 2026-03-29 - US-003
- Implemented Marshal function and Marshaler interface for encoding Go structs to DynamoDB items
- Files changed: marshal.go (new), marshal_test.go (new), prd.json (updated passes), progress.txt
- **Learnings for future iterations:**
  - All numeric types (int/int8/.../uint64, float32/float64) encode as TypeN with string representation
  - time.Time uses RFC3339 (ISO 8601) by default, unix timestamp (int64) when tagged with `unixtime`
  - Pointer fields: nil -> omit entirely; non-nil -> dereference and encode the value
  - []byte is special-cased as TypeB (binary), not treated as a slice of uint8
  - Set encoding (SS/NS/BS) requires the `set` tag option on the field
  - Nested structs use the same getCodec() cache path, encoded as TypeM
  - Maps must have string keys; non-string key maps return ErrValidation
  - Marshaler interface checked on both value and pointer receivers
---

## 2026-03-28 - US-010
- Implemented error types and sentinel values
- Files changed: errors.go (new), errors_test.go (new), prd.json (updated passes)
- **Learnings for future iterations:**
  - The Error struct wraps a Sentinel + optional Cause + optional Message
  - Use wrapError(sentinel, cause) to wrap AWS SDK errors into dynago sentinels
  - Use newError(sentinel, msg) for custom message errors
  - Helper functions IsNotFound, IsCondCheckFailed, IsValidation, IsTxCancelled use errors.Is
---

## 2026-03-29 - US-002
- Implemented struct tag parser with fieldOptions, typeCodec, and sync.Map cache
- Files changed: codec.go (new), codec_test.go (new), prd.json (updated passes), progress.txt
- **Learnings for future iterations:**
  - First token in dynamo tag is the attribute name unless it's a known option (hash, range, etc.)
  - Empty tag or no tag defaults name to Go field name
  - `isKnownOption()` helper distinguishes name tokens from option tokens
  - `getCodec()` uses `LoadOrStore` for safe concurrent first-access
  - Unexported fields are filtered at codec-build time, not at marshal/unmarshal time
---

## 2026-03-29 - US-005/US-006
- Implemented expression AST node types and builder with placeholder parser
- Files changed: internal/expr/ast.go (new), internal/expr/builder.go (new), internal/expr/builder_test.go (new), internal/reserved.go (new), unmarshal.go (fixed duplicate timeType), prd.json (updated passes)
- **Learnings for future iterations:**
  - Node interface uses private `node()` marker to prevent external implementations
  - CompareOp, LogicalOp, UpdateAction are int enums with String() methods
  - Tokenizer handles dotted paths as single tokens (e.g. "Address.City")
  - Parser implements recursive descent with correct precedence: OR < AND < NOT < comparison
  - `#name` prefix is stripped to get the actual attribute name in PathNode
  - Pre-existing issue: unmarshal.go had duplicate `timeType` declaration conflicting with marshal.go; fixed by removing from unmarshal.go
---

## 2026-03-29 - US-004
- Implemented Unmarshal function, Unmarshaler interface, and recursive unmarshalValue decoder
- Files changed: unmarshal.go (new), unmarshal_test.go (new), prd.json (updated passes), progress.txt
- **Learnings for future iterations:**
  - Pointer fields: allocate on non-NULL, set to zero on NULL or missing
  - Check Unmarshaler interface on pointer receiver first (CanAddr + Addr), then value receiver
  - `reflect.TypeFor[time.Time]()` used inline since linter removed the package-level var
  - Round-trip test calls Marshal (from US-003) and uses t.Skip if not available
  - Sets (SS/NS/BS) require the `set` tag option; without it, slices expect L type
  - Extra map keys not matching struct fields are silently ignored (no error)
---

## 2026-03-29 - US-007
- Implemented Expression AST DynamoDB string translator
- Files changed: internal/expr/translate.go (new), internal/expr/translate_test.go (new), prd.json (updated passes), progress.txt
- **Learnings for future iterations:**
  - translator struct tracks nameIdx/valueIdx counters and nameCache for alias reuse
  - aliasName() reuses aliases when the same attribute name appears multiple times in an expression
  - Update grouping preserves insertion order of actions using a separate order slice
  - Three public entry points: ToDynamo (general), ToDynamoUpdates (grouped updates), ToDynamoProjection (projections)
  - Round-trip test confirms ParseCondition -> ToDynamo preserves semantics
---

## 2026-03-28 - US-001
- Implemented core types and Backend interface
- Files changed: attribute.go (new), backend.go (new), dynago.go (new), key.go (new), key_test.go (new)
- **Learnings for future iterations:**
  - AttributeValue uses struct-with-Type-enum approach (not interface sum type) for simplicity
  - DynamoDB numbers are strings in AttributeValue.N (matches DynamoDB wire format)
  - Batch/Transaction request types are minimal but extensible — they use nested structs (KeysAndProjection, WriteRequest, TransactWriteItem etc.)
  - ScanIndexForward in QueryRequest is *bool to distinguish unset from false
  - Key() helper uses toAttributeValue() internal function that can be reused for marshalling
---

## 2026-03-29 - US-008/US-009
- Implemented condition/filter evaluator (Eval) and update evaluator (EvalUpdate)
- Files changed: internal/expr/eval.go (new), internal/expr/eval_update.go (new), internal/expr/eval_test.go (new), prd.json (updated passes), progress.txt
- **Learnings for future iterations:**
  - Eval dispatches on node type: CompareNode, LogicalNode, FuncNode
  - resolveNode handles PathNode (item lookup), ValueNode (direct), FuncNode for size() (returns TypeN)
  - compareValues supports TypeS (strings.Compare), TypeN (float64 parse), TypeB (bytes.Compare), TypeBOOL
  - Missing paths in comparisons return false (not error), matching DynamoDB behavior
  - contains() supports: string substring, list element membership, SS/NS set membership
  - EvalUpdate takes []Node (all UpdateNode), applies in order, returns copied item
  - SET uses setNestedPath which creates intermediate maps and does copy-on-write
  - ADD on missing path acts as SET; ADD on number does addition; ADD on sets does union
  - DELETE from missing attribute is a no-op (no error)
---

## 2026-03-29 - US-012
- Implemented Get[T] generic free function with GetOption (ConsistentRead, Project)
- Files changed: get.go (new), get_test.go (new), prd.json (updated passes), progress.txt
- **Learnings for future iterations:**
  - Cannot import `internal/expr` from `dynago` package due to import cycle (expr imports dynago for AttributeValue)
  - Projection expressions built inline via `buildProjection()` helper in get.go instead of using expr.ToDynamoProjection
  - Previous agent left many broken untracked files (put.go, delete.go, query.go, scan.go, update.go, iter.go, condition.go) that imported internal/expr and caused the cycle; these were removed
  - stubBackend pattern: implement only the needed method, other methods panic("not implemented")
  - Get returns ErrNotFound (sentinel directly) when response Item is nil or empty map
---
